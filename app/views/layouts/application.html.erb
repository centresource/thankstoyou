<!DOCTYPE html>
<!--[if lt IE 7]>      <html class="no-js lt-ie9 lt-ie8 lt-ie7"> <![endif]-->
<!--[if IE 7]>         <html class="no-js lt-ie9 lt-ie8"> <![endif]-->
<!--[if IE 8]>         <html class="no-js lt-ie9"> <![endif]-->
<!--[if gt IE 8]><!--> <html class="no-js"> <!--<![endif]-->
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Thanks to You, Steve...</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link id="page_favicon" href="<%= image_path("icon-steve.jpg") %>" rel="icon" type="image/jpg" />
    <!-- Place favicon.ico and apple-touch-icon.png in the root directory -->
    <%= stylesheet_link_tag '//fonts.googleapis.com/css?family=Lato:300,400,700,900,400italic' %>
    <%= stylesheet_link_tag 'screen', :media => 'all' %>
    <%= csrf_meta_tags %>
    <%= javascript_include_tag :modernizr %>
  </head>

  <body class="<%= user_signed_in? ? "logged-in" : "logged-out" %>">
    <!--[if lt IE 7]>
      <p class="browsehappy">You are using an <strong>outdated</strong> browser. Please <a href="http://browsehappy.com/">upgrade your browser</a> to improve your experience.</p>
    <![endif]-->

    <%= render 'shared/navigation' %>
    <section class="content-primary">
      <%= render 'shared/flash' %>
      <div class="content-primary-wrapper">
        <%= yield %>
      </div>
    </section>
    <!--[if IE 8]>
      <%= javascript_include_tag 'ie8' %>
    <![endif]-->

    <% if !user_signed_in? %>
    <div class="modal">
      <input class="modal-state" id="modal-1" type="checkbox" />
      <div class="modal-window">
        <div id="signup-modal" class="modal-inner">
          <label class="modal-close" for="modal-1"></label>
          <h2>Sign Up</h2>
          <p class="intro">Create an account, and add a statement and story about how Steve has affected your life story.</p>
          <hr/>
          <p>To create an account / sign in, choose from the services below:</p>
          <ul class="service-logins">
            <% PROVIDERS.each do |provider, provider_name| %>
              <li>
                <%= link_to "Sign in with #{provider_name}", user_omniauth_authorize_path(provider), :class => "zocial #{provider_name.downcase}" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
    <% end %>

    <footer class="footer-primary">
      <div class="credits">
        Made with much love by Chip Hayner and the help of his awesome coworkers at <%= link_to("Centresource Interactive Agency", "http://centresource.com") %>
      </div>
    </footer>

    <%= javascript_include_tag 'application' %>
    <%= yield :javascript %>
    <%# all page-specific elements go into content_for(:body_close) %>
    <%= yield :body_close %>
  </body>
</html>
