<section id="home-header">
  <div id="intro">
    <div id="hero">
      <%= image_tag('hdr-steve.jpg'); %>
    </div>
    <div id="hero-copy">
      <h1>Thanks to you, <strong>Steve&hellip;</strong></h1>
    </div>
  </div>
  <div id="homepage-about">
    <!-- <h2>A bit about Steve</h2> -->
    <div class="blurb">
      Steve has meant so much to so many people in so many different ways. This is a site to collect all the ways that Steve has been a part of other people's life stories.
    </div>
    <% if user_signed_in? %>
      <div class="modal">
        <label for="modal-2">
          <div class="btn btn-primary">Add Your Thanks</div>
        </label>
        <input class="modal-state" id="modal-2" type="checkbox" />
        <div class="modal-window">
          <div class="modal-inner">
            <label class="modal-close" for="modal-2"></label>
            <%= render "posts/form", :post_action => 'create' %>
          </div>
        </div>
      </div>
    <% else %>
      <div class="modal">
          <label for="modal-1">
            <div href="javascript:void(0)" class="btn btn-primary">Add Your Thanks</div>
          </label>
        </div>
    <% end %>
    <div class="modal">
      <label for="modal-3">
        <div class="btn btn-primary">Invite Others</div>
      </label>
    </div>
  </div>
</section>

<section class="post-feed">
  <div class="column-width"></div>
  <div class="gutter-width"></div>
  <% @posts.each_with_index do |post, index| %>
    <%= render "posts/post", :post => post, :post_index => index %>
  <% end -%>
</section>
<% if @post_pages > 1 %>
  <a href="javascript:void(0)" id="load-more-posts" data-page="2" data-total-pages="<%= @post_pages %>" class="btn">Load more Posts</a>
<% else %>
  <a href="javascript:void(0)" id="load-more-posts" data-page="2" data-total-pages="<%= @post_pages %>" class="btn disabled">No more Posts</a>
<% end %>
